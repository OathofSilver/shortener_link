# 高并发短链接服务 — 简历条目（依据 Feed 项目风格画像重写）

> 风格依据：`C:\Users\SF\Desktop\简历-Feed项目-风格画像与条目.md` 第一章「风格画像」（动词开头 + 手段 → 机制 → 效果 三段推进；技术名词集中句首、保留英文原词；数字只出现在实现描述段尾的压测四件套；bullet 40~70 字、一主题、效果指向可命名的工程问题；中文完成时态、全篇无第一人称与「负责/参与」）。
> 事实依据：本仓库 README.md 与实际代码（sequence/segment.go、sequence/checkpoint.go、pkg/mq/rabbitmq.go、internal/consumer/click_consumer.go、internal/logic/*.go、model/*.go），逐条核对，未使用未经证实的工具名或数字。

---

## 一、风格要点（沿用 Feed 画像，仅列执行约束）

- 开头动词：使用 / 采用 / 通过 / 基于，无主语，无「负责、参与、协助」。
- 单条结构：手段（3~6 个英文技术名词）→ 机制 → 效果（中文动宾，指向具体工程问题，如「缓存穿透」「重复计数」「主链路延迟」）。
- 分句：逗号递进到效果，分号并列第二组机制；单条 40~70 字、2~4 个分句。
- 量化：bullet 内不写数字与百分比；数字只出现在「实现描述」段尾的压测结论（并发数 + 吞吐 + 延迟分位 + 错误率），暂无数据则用【待补充】占位。
- 实现描述：150~200 字长段，业务背景 → 核心价值 → 技术难点 → 模块清单 → 压测结论，用「；」串成一段。

---

## 二、简历条目（可直接粘贴）

**高并发短链接服务**

技术栈：Go、go-zero、MySQL、Redis、RabbitMQ

实现描述：面向长链接压缩与跳转统计场景的短链服务，解决短码全局唯一、跳转高峰下数据库压力大与点击计数易重易丢等问题；核心价值在于通过转链、短码跳转与访问统计三条链路，提供稳定、低延迟的短链生成与跳转能力；难点集中在号段发号器的不重发与崩溃恢复、缓存穿透与击穿防护、点击计数的异步可靠落库；完成转链、跳转、统计三个接口及发号器、布隆过滤器、RabbitMQ 可靠投递与消费幂等等模块，并为号段发号器补充基于 miniredis 的双缓冲切换、崩溃恢复与故障降级单元测试，压测【待补充：压测工具、并发数、RPS、P95 延迟、错误率】

- 使用 Go、go-zero 和 MySQL 搭建转链、跳转、统计三条链路，通过 MD5 唯一索引查重与防循环转链，避免同一长链接重复占用短码与无效链接入库
- 采用乱序 Base62 字符表与短码黑名单生成短码，配合 HTTP 连接池复用与可达性探测，避免短码被顺序遍历猜测并过滤不可达长链接
- 采用号段模式实现发号器：Redis INCRBY 批量取号段、本地双缓冲预加载，热路径 atomic 无锁自增，避免每次取号都访问 Redis 与数据库
- 通过 DB checkpoint 以 GREATEST 幂等只增、先落盘后激活号段，启动时取 DB 与 Redis 计数的较大值对齐，保证崩溃恢复只跳号、绝不重发
- 使用 Redis bitset 布隆过滤器前置拦截不存在的短码，重启后从 MySQL 分页回灌全量短码；配合行缓存 singleflight 合并回源，减少缓存穿透与击穿对数据库的影响
- 通过 RabbitMQ 异步投递点击事件，投递置于查询成功后避免布隆假阳性误计数；配合 publisher confirm、落盘补偿与断线重连，保证消息不丢并降低跳转主链路延迟
- 通过 Redis SETNX 幂等标记与 MySQL ON DUPLICATE KEY UPDATE 原子累加计数，失败重试耗尽进死信队列、prefetch 限流，解决重复消息与消费实例宕机下的计数一致性
- 通过 Redis 计数器与 MySQL 计数表实现访问统计，未命中回源并以 SETNX 回填，避免旧值覆盖消费端刚累加的新计数

### 与样本条目的映射及条数说明
- 样本 Feed 项目 6 条 → 本稿 8 条：第 1、5、6、7 条分别对应样本的「分层 + 多级缓存」「singleflight 防击穿」「异步落库 + 幂等」；样本其余条目（推荐排序、播放体验）在本项目无对应实现，未伪造。
- 多出的 3 条（短码生成、号段发号器两条）是本项目真正的难点与差异点：发号器拆成「热路径」与「checkpoint 恢复」两条，因为「先落盘后激活」是面试必追问的决策点，单独成条更好讲。
- 若需压回 6 条：第 2 条并入第 1 条，第 4 条并入第 3 条，第 8 条并入第 7 条。

---

## 三、待补充清单

1. **压测数据（P0，实现描述段尾）**：工具（wrk / hey / ab）、并发数、RPS、P95、错误率，按样本格式补「在 X 并发下达到约 Y RPS，P95 均为 Z ms，请求错误率 N%」。建议分别压 `POST /convert` 与 `GET /:shortUrl`（跳转是热路径，单独的数据更有说服力）。
2. **项目名与时间（P1）**：样本有产品名（GoFeed），本服务建议也起一个（如 GoLink、SLink），并补项目起止时间。
3. **规模数据（P1）**：布隆过滤器 bit 数（README 记约 2000 万 bit，可换算误判率）、短码长度（Base62 后实际几位）、累计转链量 / 点击量（如有）。
4. **部署与可观测（P1）**：是否用 Docker / Docker Compose，是否接 Prometheus、Grafana 或 pprof——有则写进实现描述的模块清单（样本正是这样写的），没有就不写。
5. **职责边界（P2）**：是否独立完成全部模块，面试官会追问分工。
6. **发号器降级口径（P2）**：Redis 不可用 → 号段耗尽后 fail fast；DB 不可用 → 只阻塞新号段激活。简历放不下，但建议准备口头版本。

---

## 附：STAR 版（备用场景，用于项目展示 / 面试自述，非简历格式）

**S 情境｜**短链接服务需扛住转链、跳转、统计三链路高并发，难点是发号不重复、跳转不穿透击穿数据库、计数不重不丢。

**T 任务｜**我独立完成设计开发，负责发号器、缓存与统计链路落地。

**A 行动｜**①号段发号器：INCRBY 批量取号 + 双缓冲，checkpoint 以 GREATEST 只增、先落盘后激活，热路径无锁发号，崩溃只跳号不重发；②bitset 布隆过滤器防穿透，行缓存 singleflight 防击穿；③计数经 RabbitMQ 异步化，publisher confirm + 落盘补偿保证不丢，SETNX 幂等 + 原子累加保证不重。

**R 结果｜**三条链路交付，发号由每次一次 DB 写入降为内存发号；【需补充：压测 QPS、P95、错误率、DB 降幅】。
